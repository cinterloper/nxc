nodejs index.js --help

  Usage: index [options]

  Options:

    -h, --help             output usage information
    -V, --version          output the version number
    -c, --connect <value>  connect to host:port
    -n, --channel <value>  connect channel name
    -l, --listen           listen
    -p, --publish          publish

